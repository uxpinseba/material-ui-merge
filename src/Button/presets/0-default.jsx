import * as React from "react";
import Button from "../Button";

export default (
  <Button
    uxpId="action1"
    size="large"
    variant="contained"
    color="primary"
    fullWidth>
    Sign Up
  </Button>
);
